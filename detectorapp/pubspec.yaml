name: detectorapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # --- DEPENDENCIAS MODERNAS Y COMPATIBLES ---
  image_picker: ^1.1.1
  image: ^4.1.3
  tflite_flutter: ^0.12.1 # Versión moderna para evitar el error 'UnmodifiableUint8ListView'
  path_provider: ^2.1.2
  google_mlkit_pose_detection: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/modelo_escoliosis_final.tflite
    - assets/models/metadata_flutter.json
